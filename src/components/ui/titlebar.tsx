// ── TITLEBAR COMPONENT ───────────────────────────────────────

export function TitleBar() {
  return (
    <div
      data-tauri-drag-region
      className="h-9 w-full shrink-0 select-none bg-background/40 backdrop-blur-2xl"
    />
  );
}
